@model IEnumerable<Capa_Presentacion.Models.ReporteFormularioCIIE>

@{
    ViewBag.Title = "ReporteCantidadTipoUsuario";
    Layout = "~/Views/Shared/_Administrador.cshtml";
}
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div>
                <div class="box-header">
                    <h3 class="box-title">Reporte de la cantidad de usuarios por fechas de ingreso del formulario CIIE</h3>
                </div>
                <!-- /.box-header -->
                <div class="box-body">
                    <table id="example2" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.tipoDespacho)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.cantidad)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.porcentaje)
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.tipoDespacho)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.cantidad)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.porcentaje)%
                                    </td>
                                </tr>
                            }
                        </tbody>
                        <tfoot>
                            <tr>
                                <td>
                                    @Html.ActionLink(" Descargar reporte", "ReporteUsuariosFechaIngresoCIIEXLS", null, new { @class = "btn btn-success btn-sm fa fa-file-excel-o" }) |
                                    @Html.ActionLink(" Descargar reporte", "ReporteUsuariosFechaIngresoCIIEPDF", null, new { @class = "btn btn-danger btn-sm fa fa-file-pdf-o" })
                                </td>
                            </tr>
                        </tfoot>
                    </table>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
    <!-- /.row -->
</section>
